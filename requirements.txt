# Dependencies are declared in pyproject.toml. This file remains so that
# `pip install -r requirements.txt` does the right thing rather than
# installing a stale copy of the list.
#
#   pip install -e .            the package, plus the `music` command
#   pip install -e '.[reference]'   adds matchering for `master --reference`
#
# Python 3.12 or newer, with ffmpeg and ffprobe on PATH
# (macOS: brew install ffmpeg).

-e .
