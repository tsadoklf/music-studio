"""Numbers into words.

Verdicts and reports, the dated timeline, the advice a model gives, and the
translation from plain language into equaliser bands.

This is the only package that needs an API key, which is the reason it is its
own package: if a module here fails, the measuring half is unaffected.
"""
